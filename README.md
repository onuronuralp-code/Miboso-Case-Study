# Miboso Case Study

This repository contains both the frontend and backend for the Miboso Case Study project.

## Project Structure

- `frontend/`: Next.js application for the reservation platform.
- `backend/`: Node.js/Express server with SQLite database.

## Getting Started

Refer to the individual `README.md` files in the `frontend/` and `backend/` directories for specific setup instructions.
